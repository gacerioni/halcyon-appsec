FROM nginx:1.27-alpine
COPY dashboard/ /usr/share/nginx/html/
RUN printf 'server { listen 80; root /usr/share/nginx/html; location / { try_files $uri $uri/ /index.html; add_header Cache-Control "no-store"; } }\n' > /etc/nginx/conf.d/default.conf
