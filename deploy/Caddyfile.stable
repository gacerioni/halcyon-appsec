{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip

	handle_path /ops* {
		reverse_proxy ops:80
	}

	handle {
		reverse_proxy site-stable:3001 {
			health_uri /healthz
			health_interval 5s
		}
	}

	log {
		output stdout
		format console
	}
}
